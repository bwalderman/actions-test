name: Manual workflow

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Runs Tests
        run: |
          echo "Hello World!""
        env:
          DISPLAY: :99
